# EmbedForge 개발 이력 및 변경 전 검토 규칙

이 문서는 프로그램의 문제, 판단, 수정 및 검증 이력을 보존하는 기준 기록이다.
새 기능 또는 오류 수정은 이 문서의 규칙을 따른다. 펌웨어 입력 저장소는 분석
대상일 뿐이며, EmbedForge는 그 저장소에 Push, 브랜치 생성, 설정 변경 또는 삭제를
수행하지 않는다.

## 변경 전 필수 절차

1. **현상 기록**: 사용자가 본 오류 메시지, 재현 조건, 영향을 받는 배포 형태를 기록한다.
2. **원인 확인**: 추측과 확인된 사실을 구분하고, 관련 코드·패키지·외부 API를 점검한다.
3. **선택안 제시**: 기능 영향 또는 호환성 위험이 있는 수정은 한 가지 방법만 강요하지 않고
   최소 두 가지 방안과 장단점을 제시한다. 사용자가 특정 방향을 승인한 경우에도 기록한다.
4. **영향 범위 검토**: PySide6 버전, 분석 기능, 기존 설치/제거, 펌웨어 대상 저장소의
   비변경 원칙에 영향이 없는지 확인한다.
5. **수정·검증**: 단위 테스트, 패키지 빌드, 실제 패키지 실행/핵심 DLL 검증을 통과한 뒤에만 배포한다. 이 중 하나라도 실패하면 Release를 게시하지 않는다.
6. **배포 이력 추가**: GitHub Push, 태그, Release 자산, SHA-256 및 설치 링크를 남긴다.

## 고정 안전 원칙

- `PySide6==6.10.1`은 사용자의 명시적 승인 없이 교체하거나 하향/상향하지 않는다.
- ‘배포’는 공식 저장소 Push, 버전 태그, GitHub Release, 설치형·포터블 자산과 SHA-256 게시까지를 뜻한다.
- 외부 펌웨어 저장소는 기본 분석 대상으로만 쓰며, 내용을 수정하지 않는다.
- 배포 생성물은 설치형, 폴더형 포터블, 단일 EXE 포터블을 함께 제공한다.

## 문제 및 조치 이력

### 2026-09 — PySide6 QtCore DLL 로딩 실패

- **현상**: 설치형, 폴더형 포터블 및 단일 EXE 포터블에서 `ImportError: DLL load failed while importing QtCore` 또는 `WinError 127`가 발생했다.
- **확인 원인**: 패키징 과정에서 Poppler의 `icuuc.dll`, `icudt78.dll`(ICU 78 ABI)이 Qt 번들에 섞였다. 이 DLL은 QtCore 초기화 전에 잘못 로드되어 실행을 막았다.
- **검토한 선택안**: PySide6 교체, 폴더형 전환, 실행 시 DLL 경로 보정, 빌드 입력 DLL 격리.
- **채택**: PySide6 6.10.1을 유지하고, Poppler 경로를 빌드 중 제외하며, 결과 번들에 혼입된 두 DLL을 제거하고 Qt import smoke test로 검증한다.
- **재발 방지 조치**: 배포 스크립트는 Poppler 경로를 빌드 입력에서 제외하고, 최종 번들에 혼입된 두 DLL을 제거한 뒤 잔존 여부를 확인한다. 이어서 실제 동결 EXE를 `--smoke-test`로 실행해 `QtCore` import 성공 표식이 없으면 Release 생성을 차단한다. Qt/clang 시험 프로세스가 종료되지 않는 호스트에서는 성공 표식 확인 후 시험 프로세스만 정리한다. 포터블·설치형은 같은 검증 완료 번들을 사용한다.

### 2026-09 — 성능 검토 (구현 보류)

- **질문**: Python으로 만든 분석 프로그램의 초기 로딩·분석 전환이 느린 문제를 개선할 수 있는가.
- **결론**: Python 자체가 개선 불가능한 원인이 아니다. UI 스레드의 동기 분석, 전체 재탐색, 대형 트리의 과도한 렌더링이 주요 위험 요인이다.
- **향후 검토 우선순위**: 지연 초기화, 파일 해시 기반 증분 분석, 분석 캐시, 보이는 행만 렌더링하는 가상화, I/O·파싱의 백그라운드 분리를 계측 결과에 따라 적용한다.
- **이번 배포 범위**: 검토·기록만 한다. 성능 관련 동작 변경은 포함하지 않는다.

### 2026-09 — 업데이트 확인 시 레거시 설치 파일명 참조

- **현상**: 프로그램 내부 업데이트가 `C-Call-Hierarchy-Explorer-Setup-*` 파일명만 우선 참조할 수 있었다.
- **원인**: EmbedForge로 이름을 변경한 뒤 최신 Release 자산명 탐색이 완전히 전환되지 않았다.
- **조치**: `EmbedForge-Setup-{version}.exe`를 우선 사용하고, 이전 파일명은 하위 호환용으로만 허용하도록 수정했다.
- **검증**: Release 메타데이터·SHA-256·파일 크기 검사와 단위 테스트를 통과해야 한다.

### 2026-09 — GitHub Repository 생성 HTTP 404

- **현상**: 개인 Dashboard 주소에서 Repository 생성 시 HTTP 404가 표시됐다.
- **확인 원인**: 개인 계정 생성 요청을 읽기 전용 성격의 `/users/{login}/repos`로 보내고 있었다. GitHub의 개인 계정 생성 API는 `/user/repos`이다.
- **조치**: 개인 Dashboard는 `/user/repos`, 조직 Dashboard는 `/orgs/{owner}/repos`를 사용하도록 분기했다. HTTP 404에는 Dashboard 유형, 토큰 `Administration: Write`, 조직 Fine-grained 토큰 승인 여부를 안내한다.
- **토큰 원칙**: Classic 토큰은 필수가 아니다. Fine-grained 토큰도 `Administration: Write` 권한과 조직 정책상 승인이 있으면 사용할 수 있다.

### 2026-09 — IAR MAP 메모리맵 가독성 및 줄바꿈 표기

- **현상**: 밝은 영역에서 흰 글씨가 보이지 않고, 작은 영역은 사용률만·큰 영역은 2줄 상세 정보가 표시되어 형식이 달랐다. `\\n`이 글자로 노출되기도 했다.
- **조치**: 모든 영역에 `영역 · 사용/전체 B · 사용률` 한 줄 형식을 적용하고 공간이 부족하면 동일 문장을 말줄임 처리한다. 배경 밝기에 따라 어두운/밝은 굵은 글씨를 자동 선택하며, 실제·문자형 줄바꿈을 모두 공백으로 정규화한다.
- **검증 기준**: 밝고 어두운 ROM/RAM 영역 모두에서 텍스트 대비가 확보되고, 어떠한 영역에도 `\\n` 텍스트가 노출되지 않아야 한다.

## 현재 배포 준비 항목: v2.5.22

- GitHub 개인·조직 Repository 생성 API 분기 및 권한 안내
- IAR MAP 영역 라벨의 대비·굵기·한 줄 표기 통일
- 기존 Qt DLL 정제/실행 검증 체계 유지
- 위 항목 외의 미완료 기능 요청은 다음 업데이트까지 보류

### 2026-09 — 소스 인코딩 일괄 변환: v2.5.23

- **목표**: IAR 펌웨어 소스의 CP949/EUC-KR·UTF-8 BOM 파일을 UTF-8 무 BOM으로 안전하게 일괄 변환한다.
- **위험 검토**: CP949는 다른 레거시 바이트열도 해석할 수 있어, 단순 성공만으로 변환하면 파일을 손상시킬 수 있다.
- **채택**: UTF-8 strict와 BOM을 먼저 판별하고, CP949 해석 결과에 한글 근거가 있을 때만 변환 후보로 표시한다. 알 수 없는 인코딩·바이너리·빌드 폴더 파일은 자동 변환하지 않는다.
- **원본 보존**: 원본 위치 변환은 `.bak` 백업 후 임시 파일을 원자적으로 교체하며, 별도 출력 폴더 변환도 선택할 수 있다. 실패한 파일은 원본을 건드리지 않는다.
- **형식 보존**: 기존 CRLF/LF/CR 줄바꿈은 유지하며, 결과 UTF-8에는 BOM을 붙이지 않는다.
- **검증**: UTF-8·UTF-8 BOM·CP949·불확실 파일·빌드 폴더 제외·백업·별도 출력 폴더·줄바꿈 보존 테스트를 포함한다.

### 2026-09 — GitHub 최초 업로드 응답 없음: 수정 전 검토

- **현상**: Repository 생성 후 최초 코드 업로드를 선택하면 UI 스레드에서 파일별 GitHub 요청을 순차 실행해 창이 응답 없음 상태로 유지된다.
- **원인**: 네트워크 전송·파일 읽기·각 파일의 GitHub Contents API 요청이 대화상자 이벤트 루프를 막는다. 대형 IAR 프로젝트에서는 요청 수와 30초 타임아웃이 누적된다.
- **채택**: 업로드·저장소 목록 조회를 백그라운드 작업으로 분리하고, 진행률·현재 파일·성공/실패 요약을 표시한다. 첫 업로드 기능은 커밋 없는 저장소만 명시적으로 활성화한다.
- **상태 보존**: Dashboard별 최근 주소·로컬 폴더·기본 입력값을 저장하며, 토큰은 Windows 사용자 계정 범위 암호화 저장을 우선한다. UI에는 마스킹된 값만 표시한다.
- **진행 가시화**: Repository 생성, 파일 검사, 커밋 준비, 파일 전송, 커밋 검증, 진행 마무리로 분리한다. 실패 시 해당 원형 노드를 빨간색으로 고정하고 단계·마지막 파일·GitHub 오류를 상태 영역에 남긴다.
- **v2.5.26 전환**: Contents API의 파일별 PUT/커밋 경로를 최초 업로드에서 제거하고, 임시 폴더 clone → 제외 파일 복사 → 단일 Git commit → Git push로 전환한다. Git 인증 토큰은 URL·명령행·로그에 넣지 않고 임시 AskPass와 환경 변수로만 전달한다.
- **v2.5.25 UI 보완**: 넓은 좌·우 패널과 최소 폭을 적용해 로컬 IAR 경로와 Repository 목록을 표시한다. 작업 모드는 라디오 버튼으로 화면을 전환하지 않고, 빈 저장소 선택 시 생성 전용 입력을 비활성화하는 방식으로 유지한다. 저장된 토큰이 있는 Dashboard 선택은 목록을 자동 조회한다.

### 2026-09 — Git 업로드 중단 및 자동 복구: v2.5.26

- **현상**: 대형 IAR 소스 전송 중 닫기 버튼을 누르면 대화상자만 닫히고 백그라운드 작업의 상태를 확인하거나 안전하게 멈출 방법이 없었다. 중단 후 다시 올릴 때 원격에 일부 남은 파일의 처리 기준도 없었다.
- **확인 원인**: Git 전송 프로세스에 취소 신호가 전달되지 않았고, 원격 파일과 로컬 파일을 경로·내용 기준으로 비교하는 복구 단계가 없었다.
- **채택**: 닫기 요청은 즉시 창을 닫지 않고 ‘중단 중’ 상태와 진행바를 표시한다. 파일 복사 및 각 Git 단계가 취소 신호를 확인하고, 실행 중인 Git 프로세스는 종료한 뒤에만 창을 닫는다.
- **자동 복구 기준**: 동일 상대 경로와 SHA-256이 일치한 원격 파일은 다시 전송하지 않는다. 누락 파일만 기존 원격 이력을 보존한 단일 Git 커밋으로 Push한다. 같은 경로의 SHA-256이 다르면 자동 덮어쓰기를 하지 않고 충돌로 중단한다. README 등 로컬 업로드 대상이 아닌 원격 파일은 보존한다.
- **중단 후 원격 상태**: clone·파일 준비·commit 전 중단은 원격 변경이 없다. push 중 중단은 네트워크 시점에 따라 Push가 완료되었을 수 있으나, 다음 실행에서 clone과 SHA-256 비교를 통해 이미 반영된 파일은 Push 없이 완료되고 누락 파일만 복구한다.
- **검증**: 취소 사전 확인, 동일 파일 Push 생략, Git 단일 커밋 경로 및 전체 회귀 테스트를 통과해야 한다.
