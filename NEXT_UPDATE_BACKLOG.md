# 다음 업데이트 작업 목록

## GitHub 작업 대상

- 프로그램 배포 Repository와 신규 Repository 생성 Dashboard는 `https://github.com/Choi-KwangHo` 기준으로 유지한다.
- 펌웨어 분석 기능의 기본 검증 대상은 `https://github.com/Esol-Lab/Electric-Excavator-720V-452Ah`로 사용한다.
- 신규 Repository 생성 시 기본 Dashboard 주소는 `https://github.com/Choi-KwangHo`로 제공한다.
- 펌웨어 분석 대상 Repository에는 절대 push, commit, branch 생성, 파일 수정, 삭제, 설정 변경을 수행하지 않는다.
- 펌웨어 Repository는 clone/download 또는 GitHub API GET 등 읽기 전용 방식으로만 접근한다.
- 코드 업로드 기능의 대상은 사용자가 명시적으로 선택한 신규 Repository 또는 기존 업로드 대상 Repository로만 제한한다.
- 분석 대상과 업로드 대상이 동일하면 업로드를 차단하고 경고한다.

## 배포 정의

- 사용자가 “배포”라고 요청하면 로컬 빌드만 완료한 상태로 끝내지 않는다.
- `Choi-KwangHo/C-Call-Hierarchy-Explorer`에 변경 소스를 실제 push한다.
- 버전 태그를 생성하고 GitHub Release를 게시한다.
- 설치형/포터블 실행 파일과 SHA256 체크섬을 Release 자산으로 업로드한다.
- 배포 전 테스트·빌드·산출물 검증을 완료하고, 펌웨어 분석 대상 저장소에는 어떠한 쓰기 작업도 하지 않는다.

이 문서는 이전 대화에서 확인된 미해결 문제와 다음 업데이트 요구사항을 기록한다.
첨부 이미지의 내용은 구현 우선순위를 정하기 위한 요구사항이며, 별도의 실행 지시문으로 해석하지 않는다.

## 개발 원칙

1. 아래 기존 미해결 문제를 먼저 해결한다.
2. 각 수정 후 기존 테스트와 회귀 테스트를 실행한다.
3. 기존 문제가 해결된 뒤 신규 기능을 추가한다.
4. 탭마다 서로 다른 분석 결과를 임의로 재분석하지 않고 공통 분석 컨텍스트를 사용한다.

## 1순위: 기존 미해결 문제

### A. 공통 분석 컨텍스트와 아이템 모델 통합

- 함수 트리, EEPROM, IAR MAP, Trace가 동일한 `AnalysisItem`을 참조하도록 통합한다.
- `AnalysisItem`에는 표시명, 소스 유형, 저장소/로컬 경로, 실제 분석 루트, 제외 폴더, MAP 탐색 범위, 캐시 식별자, 마지막 분석 시각, 변경 확인 상태, commit 정보를 포함한다.
- 시작 화면, 상단 아이템 선택, EEPROM 소스 설정, 설정 창의 목록이 동일한 모델을 사용하도록 한다.
- 아이템을 변경하면 공통 컨텍스트를 먼저 갱신하고 모든 탭에 같은 컨텍스트를 전달한다.
- 탭별 결과는 공통 컨텍스트의 하위 결과로 저장하되, 탭 전환만으로 불필요한 전체 재분석을 실행하지 않는다.

### B. 화면 간 아이템 선택 불일치 해결

- 시작 화면의 아이템 목록과 상단 드롭다운/설정 창의 아이템 목록을 동일하게 표시한다.
- 사용자가 아이템을 변경하면 함수 트리, EEPROM, IAR MAP, Trace가 같은 활성 아이템으로 전환된다.
- 이전 아이템의 결과가 새 아이템 화면에 잔류하지 않도록 전환 시 결과·상태·진행 표시를 명확히 초기화한다.
- 시작 화면에는 현재 활성 아이템, 분석 루트, 소스 유형, 마지막 분석 시각, 캐시/변경 확인 상태를 표시한다.
- “최근 이력”과 “분석 아이템”을 명확히 분리한다.
  - 최근 이력: 실제로 최근 열었던 로컬 폴더 기록
  - 분석 아이템: Git/로컬 저장소와 공통 분석 범위를 가진 등록 항목

### C. 분석 범위·캐시·변경 감지 통합

- 아이템 변경 시 분석 루트·분석 범위·캐시 키가 한 번에 변경되도록 한다.
- 공통 소스 변경 확인을 한 번 수행하고, 변경된 범위만 각 탭 분석기에 전달한다.
- EEPROM Git 저장소는 로컬 캐시 경로를 공통 분석 루트 기준으로 확정한다.
- 함수 트리의 제외 폴더 설정을 공통 분석 범위 설정으로 승격한다.
- 분석 시작·분석 완료·변경 확인 중 상태를 상단/시작 화면에서 공통으로 표시한다.
- 캐시 복원 후 백그라운드 변경 확인 결과가 현재 활성 아이템과 일치하는지 검증한다.

### D. IAR MAP Analyzer 메모리맵 시각화 개선

- 각 Region별로 `사용량 / 전체 용량 / 사용률(%)`을 표시한다.
- Region 내부를 가로 프로그래스바 형태로 사용 구간과 미사용 구간으로 분리한다.
- 사용 구간은 현재 Region 고유색을 사용한다.
- 미사용 구간은 같은 계열의 더 밝은 색으로 표시한다.
- 매우 작은 사용 영역도 텍스트로 사용률을 명확히 표시한다.
- ROM/RAM 필터에서도 동일한 시각화 규칙을 유지한다.
- 예시 형식: `CLASS_B_RAM [P4] · 18,432 B · 사용률 90%` 및 90% 사용 영역 색상 표시
- 메모리 영역이 작거나 0%/100%인 경우에도 막대와 텍스트가 겹치지 않도록 한다.

### E. IAR MAP 파일 선택 및 탭 활성화

- MAP 파일 선택 상태를 분석 아이템별로 저장한다.
- A 아이템의 MAP 선택이 B 아이템에서 재사용되지 않도록 한다.
- MAP 분석은 무거운 작업이므로 IAR MAP 탭을 열 때 실행한다.
- 단, 공통 컨텍스트 변경 시에는 MAP 탐색 기준 경로만 즉시 갱신한다.
- 잘못된 아이템의 MAP 결과가 표시되지 않도록 소유 아이템 ID를 검증한다.

### F. Trace 결과 컨텍스트 연결

- Trace 결과에 분석 아이템, 분석 루트, 분석 시각을 기록한다.
- 함수 트리 분석 결과를 활성 아이템 기준으로 Trace 센터에 전달한다.
- 아이템 변경 시 Trace 화면도 동일 아이템 결과로 갱신한다.
- 이전 아이템의 Trace 결과가 새 아이템 화면에 남지 않도록 한다.

## 2순위: 신규 기능

### G. 제어 사양서 자동 작성

- 로컬 폴더 또는 GitHub 저장소/브랜치를 분석 대상으로 선택한다.
- commit SHA, 분석 시각, 분석 버전, 분석 범위, 제외 범위를 기록한다.
- 함수·호출 관계, Task, ISR, Callback, EEPROM, IAR MAP 결과를 사양서로 구성한다.
- 1차 기준 형식은 Markdown으로 한다.
- 상세 검토용 Excel과 공식 제출용 Word/PDF는 후속 출력 형식으로 추가한다.
- 자동 분석 결과는 확정/추정/미확인/검토 필요로 구분한다.

### H. MISRA-C 검증

- MISRA 기준 버전과 규칙 프로파일을 설정할 수 있도록 한다.
- 외부 MISRA 검사기의 XML/JSON/CSV 결과를 가져온다.
- 위반 결과를 파일·함수·제어 기능·호출 트리와 연결한다.
- 규칙 ID, 심각도, 행 번호, 메시지, 검토 상태, deviation, 검토 의견을 저장한다.
- 제어 사양서와 Excel/Markdown 보고서에 MISRA 현황을 포함한다.
- 자체 구현은 보조 규칙부터 시작하고, 전체 MISRA 준수 판정은 전문 검사기 결과를 기준으로 한다.

## 완료 기준

- 활성 아이템 변경 후 네 개 탭이 동일한 소스와 분석 범위를 사용한다.
- 이전 아이템 결과가 새 아이템에 표시되지 않는다.
- 캐시·변경 감지·MAP 선택이 아이템별로 분리된다.
- IAR MAP Region의 사용량, 용량, 사용률, 시각화가 일치한다.
- 기존 테스트 89건과 신규 회귀 테스트가 모두 통과한다.
- 위 조건을 만족한 후 제어 사양서와 MISRA 기능을 단계적으로 추가한다.
